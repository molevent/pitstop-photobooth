version: '3.8'

services:
  backend:
    build: ./server
    ports:
      - "3001:3000"
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped

  frontend:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
